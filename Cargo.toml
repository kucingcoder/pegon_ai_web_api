[package]
name = "pegon_ai"
version = "1.0.0"
edition = "2024"

[dependencies]
rocket = { version = "0.5.1", features = ["json"] }
sea-orm-migration = { version = "^1.0.0", features = [
    "sqlx-mysql",
    "runtime-tokio-rustls",
] }

[workspace]
members = [".", "migration"]
